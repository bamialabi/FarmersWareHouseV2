@model IEnumerable<Core.DataAccess.tbl_LK_MarketProduct>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_LK_MarketUnitOfMeaure.UnitOfMeasureName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_LK_MarketUnitOfMeaure.UnitOfMeasureName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductID })
             @*|
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductID })*@
        </td>
    </tr>
}

</table>
