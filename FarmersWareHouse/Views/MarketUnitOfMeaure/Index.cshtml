@model IEnumerable<Core.DataAccess.tbl_LK_MarketUnitOfMeaure>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UnitOfMeasureName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UnitOfMeasureName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UnitOfMeasureID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UnitOfMeasureID })
             @*|
            @Html.ActionLink("Delete", "Delete", new { id=item.UnitOfMeasureID })*@
        </td>
    </tr>
}

</table>
