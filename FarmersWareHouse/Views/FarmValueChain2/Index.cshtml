@model IEnumerable<Core.DataAccess.tbl_LK_FarmValueChain>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ValueChainName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ValueChainName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ValueChainID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ValueChainID }) @*|
            @Html.ActionLink("Delete", "Delete", new { id=item.ValueChainID })*@
        </td>
    </tr>
}

</table>
