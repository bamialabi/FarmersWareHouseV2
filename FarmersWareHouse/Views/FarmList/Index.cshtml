@model IEnumerable<Core.DataAccess.tbl_LK_FarmList>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FarmName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FarmName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FarmListID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FarmListID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FarmListID })
        </td>
    </tr>
}

</table>
